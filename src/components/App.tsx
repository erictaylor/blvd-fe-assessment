import { AppContainer } from './AppContainer';
import { LookupView } from './LookupView';

export const App = (): JSX.Element => {
	return (
		<AppContainer>
			<LookupView />
		</AppContainer>
	);
};
